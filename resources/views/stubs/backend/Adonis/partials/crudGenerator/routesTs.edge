@if(apiPublicContent)
  Route.group(() => {
      {{{ apiPublicContent }}}
    })
    .prefix('/api')
@endif

@if(apiAuthContent)
  Route.group(() => {
      {{{ apiAuthContent }}}
    })
    .middleware(['auth'])
    .prefix('/api')
@endif
